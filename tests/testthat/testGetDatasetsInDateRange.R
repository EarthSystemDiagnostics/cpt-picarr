library(testthat)

context("test the function getDatasetsInDateRange")

test_that("test", {
  
  # -------- INITIALIZE INPUTS -----------
  
  # -------- CALL FUNCTION UNDER TEST ----
  
  actual <- getDatasetsInDateRange(dateRange, project, basePath)
})